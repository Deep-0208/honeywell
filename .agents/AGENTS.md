# Agent Rules & Constraints

## AI Bot Blocking (Temporary)
**Rule**: Do NOT modify `robots.txt` to unblock `GPTBot`, `PerplexityBot`, `ClaudeBot`, or `Google-Extended`. 
**Context**: The site is currently under construction. We are intentionally blocking all AI scrapers until all pages are built and audited. Once the entire build and audit phase is complete, the user will explicitly ask to unblock them. Until then, keep them blocked.
