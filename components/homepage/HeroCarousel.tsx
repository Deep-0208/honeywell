'use client'; import React from 'react';
import Image from 'next/image'; interface HeroCarouselProps { images: Array<{ src: string; alt: string; title: string; }>;
} export function HeroCarousel({ images }: HeroCarouselProps) { const [currentSlide, setCurrentSlide] = React.useState(0); React.useEffect(() => { const timer = setInterval(() => { setCurrentSlide((prev) => (prev + 1) % images.length); }, 4000); return () => clearInterval(timer); }, [images.length]); return ( <> {images.map((item, index) => ( <div key={index} className={`absolute inset-0 transition-opacity duration-1000 ${ index === currentSlide ? 'opacity-100 z-10' : 'opacity-0 z-0' }`} > <Image src={item.src} alt={item.alt} title={item.title} fill priority={index === 0} fetchPriority={index === 0 ? "high" : "auto"} decoding="sync" quality={85} sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 600px" className="object-contain p-2 pb-[80px] sm:p-4 sm:pb-[96px] lg:p-8 lg:pb-[112px] w-full h-full" /> {/* Product label overlay */} <div className="absolute bottom-3 left-3 right-3 sm:bottom-5 sm:left-5 sm:right-5 bg-white/95 backdrop-blur-md border border-slate-200/60 rounded-xl px-4 py-3 sm:px-5 sm:py-4 shadow-sm"> <div className="flex items-center justify-between mb-1.5"> <p className="text-[11px] sm:text-xs font-bold uppercase tracking-[0.15em] text-honeywell-red font-body "> Featured Product </p> <div className="flex gap-1.5"> {images.map((_, dotIndex) => ( <div key={dotIndex} className={`h-1.5 rounded-full transition-all duration-300 ${ dotIndex === currentSlide ? 'w-4 bg-honeywell-red' : 'w-1.5 bg-slate-200' }`} /> ))} </div> </div> <p className="text-sm sm:text-base font-display font-bold text-honeywell-navy "> {item.title} </p> </div> </div> ))} </> );
}
